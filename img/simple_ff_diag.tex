\tikzset{%
  every neuron/.style={
    circle,
    draw,
    minimum size=\tikzscale*25
  },
  neuron missing/.style={
    draw=none, 
    scale=\tikzscale*1.5,
    text height=\tikzscale*5,
    execute at begin node=\color{black}$\vdots$
  },
}

\begin{tikzpicture}[x=\tikzscale*25, y=\tikzscale*45, >=stealth]

\foreach \m/\l [count=\y] in {1,2,3,4}
  \node [every neuron/.try, neuron \m/.try] (input-\m) at (0,1.625-\y*0.7) {};

\foreach \m [count=\y] in {1,2,3,missing,4}
  \node [every neuron/.try, neuron \m/.try ] (hidden1-\m) at (2,2-\y*0.7) {};

\foreach \m [count=\y] in {1,2,3,missing,4}
  \node [every neuron/.try, neuron \m/.try ] (hidden2-\m) at (4,2-\y*0.7) {};
  
\foreach \m [count=\y] in {1,2,3,missing,4}
  \node [every neuron/.try, neuron \m/.try ] (output-\m) at (6,2-\y*0.7) {};

\foreach \l [count=\i] in {,+1,+2,+3}
  \draw [<-] (input-\i) -- ++(-1,0)
    node [above, midway] {$x_{t\l}$\ \ \ \ \ };

\foreach \l [count=\i] in {1,2,3,200}
  \node at (hidden1-\i) {\small $H_{1,\l}$};
  
\foreach \l [count=\i] in {1,2,3,200}
  \node at (hidden2-\i) {\small $H_{2,\l}$};
    
\foreach \l [count=\i] in {4,5,6,203}
  \draw [->] (output-\i) -- ++(1,0)
    node [above, midway] {\ \ \ \ \ \ $y_{t+\l}$};

\foreach \i in {1,...,4}
  \foreach \j in {1,...,4}
    \draw [->] (input-\i) -- (hidden1-\j);

\foreach \i in {1,...,4}
  \foreach \j in {1,...,4}
    \draw [->] (hidden1-\i) -- (hidden2-\j);
    
\foreach \i in {1,...,4}
  \foreach \j in {1,...,4}
    \draw [->] (hidden2-\i) -- (output-\j);

\foreach \l [count=\x from 0] in {Input, Hidden, Hidden, Output}
  \node [align=center, above] at (\x*2,2) {\l \\ layer};

\end{tikzpicture}
